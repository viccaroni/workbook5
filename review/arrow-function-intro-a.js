function isOver60(arrayValue) {
    if (arrayValue > 60) {
      return true;
    } else {
      return false;
    }
  }
  
  const isOver60 = (arrayValue) => {
    if (arrayValue > 60) {
      return true;
    } else {
      return false;
    }
  };
  // ===============
  function isOver60(arrayValue) {
    return arrayValue > 60;
  }
  
  const isOver60 = (value) => value > 60;
  
  //================
  // function (arrayValue) {
  //   return arrayValue > 60;
  // }
  
  // (value, second) => value > 60;
  
  //1.
  // function keyword on the left side of parameter list is replaced by arrow (=>) 
  // on the right side of the parameter list
  
  2. 